# Core scientific stack
numpy
pandas>=2.2.2

# Jupyter tooling
jupyter

# Scientific/plotting
scipy
matplotlib
plotly
seaborn
statsmodels
tabulate
pingouin

# YAML
pyyaml

# Data cleaning helpers (pyjanitor uses pandas-flavor under the hood)
# --- PINNED to an older version that uses the deprecated pandas-flavor API ---
pyjanitor==0.26.0

# --- PINNED to a version that still contains 'register_groupby_method' (removed in 0.7.0) ---
pandas-flavor==0.6.0

# Geo stack
geopandas
folium
branca
contextily

# Machine learning
scikit-learn
